// common/constants/settings-input-type.enum.ts
export enum SettingsInputType {
  TEXT = 'text',
  TEXTAREA = 'textarea',
  EMAIL = 'email',
  PHONE = 'phone',
  URL = 'url',
  NUMBER = 'number',
  SWITCH = 'switch',
  SELECT = 'select',
  JSON = 'json',
}
